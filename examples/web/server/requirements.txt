# The origofs Python bindings with the FastAPI extra, plus an ASGI server.
#
# From a checkout you build the bindings locally instead of installing from an
# index — the extension is a compiled pyo3 module:
#
#     (cd ../../../crates/origofs-py && python -m venv .venv && . .venv/bin/activate \
#        && pip install maturin && maturin develop)
#     pip install fastapi "uvicorn[standard]"
#
# When origofs is published, this file is all you need:
origofs[fastapi]
uvicorn[standard]
