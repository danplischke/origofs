"""Alembic migrations for the origofs metadata schema (packaged, not imported).

This package is Alembic's ``script_location``: ``env.py`` and ``versions/`` are
discovered and executed by Alembic itself (see ``origofs.db.get_alembic_config``),
not imported as ordinary Python by application code.
"""
