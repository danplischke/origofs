/target
**/*.rs.bk
Cargo.lock.orig
*.db
*.db-wal
*.db-shm
.origofs-cas/
# cargo-mutants writes its per-run reports here (see .github/workflows/mutants.yml)
mutants.out/
mutants.out.old/

# maturin/pytest virtualenv for the Python bindings
crates/origofs-py/.venv/

# mkdocs build output (`mkdocs build` / `mkdocs serve`)
/site/
